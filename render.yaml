services:
  - type: web
    runtime: static
    name: orch-landing
    rootDir: site
    buildCommand: echo "No build needed, plain static HTML/CSS"
    staticPublishPath: .
    previews:
      generation: off
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
